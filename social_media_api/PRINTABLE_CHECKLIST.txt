# 🎯 QUICK DEPLOYMENT CHECKLIST

## Print This! Use During Deployment

---

## STEP 1: PREPARATION (5 minutes)

```
☐ Read QUICKSTART.md
☐ Read 00_START_HERE.md
☐ Choose deployment method:
   ☐ Docker    ← Recommended
   ☐ Heroku
   ☐ VPS
   ☐ PythonAnywhere
```

---

## STEP 2: ENVIRONMENT SETUP (5 minutes)

```
☐ Create .env file:
   cp .env.example .env

☐ Edit .env with:
   SECRET_KEY = [GENERATE NEW]
   DEBUG = False
   ALLOWED_HOSTS = yourdomain.com
   CORS_ALLOWED_ORIGINS = https://yourdomain.com

☐ Generate SECRET_KEY:
   python manage.py shell
   from django.core.management.utils import get_random_secret_key
   print(get_random_secret_key())
   # Copy output to SECRET_KEY in .env
```

---

## STEP 3: LOCAL TESTING (10 minutes)

```
☐ Test locally:
   python manage.py migrate
   python manage.py collectstatic --noinput
   python manage.py runserver

☐ Verify at:
   http://localhost:8000/api/

☐ Stop server: Ctrl+C
```

---

## STEP 4: DEPLOYMENT

### If Using DOCKER:
```
☐ Install Docker & Docker Compose
☐ Run: docker-compose up -d
☐ Run: docker-compose exec web python manage.py migrate
☐ Run: docker-compose exec web python manage.py createsuperuser
☐ Verify at: http://localhost:8000
☐ Push to production server
☐ Run on server: docker-compose up -d
```

### If Using HEROKU:
```
☐ Install Heroku CLI: heroku login
☐ Create app: heroku create your-app-name
☐ Add DB: heroku addons:create heroku-postgresql:hobby-dev
☐ Set config:
   heroku config:set SECRET_KEY=your-key
   heroku config:set DEBUG=False
   heroku config:set ALLOWED_HOSTS=your-app.herokuapp.com
☐ Deploy: git push heroku main
☐ Migrate: heroku run python manage.py migrate
☐ Create superuser: heroku run python manage.py createsuperuser
☐ Verify at: https://your-app.herokuapp.com
```

### If Using VPS (DigitalOcean/AWS/Azure):
```
☐ Follow DEPLOYMENT.md VPS section step-by-step
☐ Server setup (Python, PostgreSQL, Nginx)
☐ Configure application
☐ Setup Gunicorn
☐ Configure Nginx
☐ Install SSL certificate
☐ Start services
☐ Verify deployment
```

---

## STEP 5: POST-DEPLOYMENT VERIFICATION (10 minutes)

```
IMMEDIATE CHECKS:
☐ Application is running
☐ No errors in logs
☐ Check logs: docker-compose logs -f web

API TESTS:
☐ GET  /api/posts/ returns data
☐ POST /api/accounts/register/ works
☐ POST /api/accounts/login/ returns token
☐ GET  /admin/ is accessible

PERFORMANCE:
☐ Response time < 200ms
☐ No 500 errors
☐ No database errors
☐ No CORS errors

FUNCTIONALITY:
☐ User registration works
☐ User login works
☐ Create post works
☐ View posts works
☐ Create comment works
☐ Like post works
```

---

## STEP 6: SECURITY HARDENING (5 minutes)

```
BEFORE GOING PUBLIC:
☐ Set DEBUG = False in .env
☐ Configure ALLOWED_HOSTS
☐ Set up HTTPS/SSL
☐ Configure CORS_ALLOWED_ORIGINS
☐ Set secure database password
☐ Enable monitoring
☐ Set up backups
```

---

## QUICK COMMAND REFERENCE

### Docker Commands
```bash
docker-compose up -d              # Start
docker-compose down               # Stop
docker-compose logs -f web        # View logs
docker-compose exec web sh        # Access container
docker-compose ps                 # Status
```

### Django Commands
```bash
python manage.py migrate
python manage.py createsuperuser
python manage.py collectstatic --noinput
python manage.py runserver
python manage.py check --deploy
```

### Git Commands
```bash
git add .
git commit -m "Deploy"
git push origin main
git push heroku main              # Heroku only
```

---

## TROUBLESHOOTING QUICK FIXES

**Docker won't start?**
```
docker-compose logs -f web
```

**Database error?**
```
docker-compose exec web python manage.py migrate
```

**Static files not loading?**
```
docker-compose exec web python manage.py collectstatic --noinput
```

**Port already in use?**
```
docker-compose down
```

**CORS error?**
```
Check CORS_ALLOWED_ORIGINS in .env
```

---

## MONITORING & MAINTENANCE

### Daily
- [ ] Check logs
- [ ] Monitor errors

### Weekly
- [ ] Review performance
- [ ] Check disk usage
- [ ] Verify backups

### Monthly
- [ ] Security audit
- [ ] Update dependencies
- [ ] Database optimization

---

## IMPORTANT REMINDERS

✅ Never commit `.env` file to Git
✅ Never share SECRET_KEY
✅ Always use HTTPS in production
✅ Always set DEBUG = False
✅ Always configure ALLOWED_HOSTS
✅ Always backup database
✅ Always test locally first

---

## USEFUL LINKS (Open During Deployment)

Bookmark these:
- Django Docs: https://docs.djangoproject.com/en/5.2/
- DRF Docs: https://www.django-rest-framework.org/
- Docker Docs: https://docs.docker.com/
- Heroku Docs: https://devcenter.heroku.com/

---

## FILE QUICK REFERENCE

| Need | File |
|------|------|
| Environment template | `.env.example` |
| Dependencies | `requirements.txt` |
| Docker setup | `docker-compose.yml` |
| Detailed guide | `DEPLOYMENT.md` |
| Security info | `SECURITY.md` |
| All docs | `DOCS_INDEX.md` |

---

## TIME TRACKER

```
Preparation:        ___ minutes
Environment setup:  ___ minutes
Local testing:      ___ minutes
Deployment:         ___ minutes
Verification:       ___ minutes
Security setup:     ___ minutes
─────────────────────────────────
TOTAL TIME:         ___ minutes
```

---

## SIGN-OFF

```
I have:
☐ Read the documentation
☐ Set up environment variables
☐ Tested locally
☐ Deployed successfully
☐ Verified all functionality
☐ Configured security
☐ Set up monitoring

Status: LIVE ✅

Date: _______________
Name: _______________
```

---

**Print this page and keep it handy!**
**Need detailed instructions? Open DEPLOYMENT.md**
**Ready to start? Open QUICKSTART.md**
